package HashMap_a;

import java.util.HashMap; // import the HashMap Class

// A HashMap store items in "key/value" pairs, and you can access them
// by an index of another type (e.g a String)

// One object is used as a key(index) to another object(value). It can store
// different types: String keys and Integer values, or the same type, like: String keys and String values:

public class Main {
    HashMap<String, String> capitalCities = new HashMap<>();
}
